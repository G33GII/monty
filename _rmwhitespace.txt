#include "monty.h"

/**
* _rmwhitespace -
* @_b:
* Return: char pointer
*/
void _rmwhitespace(void)
{
    char *_b = _data.SP;
    char *_p = _b;

    while (*_b)
    {
        if (!isspace(*_b))
        {
            *_p = *_b;
            _p++;
        }
        _b++;
    }
    *_p = '\0';
}
